[
  {
    "commit_sha": "1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b",
    "pr_number": 20101,
    "family": "bert",
    "title": "Fix shape mismatch in BertSelfAttention head view",
    "body": "The transpose_for_scores function uses the wrong number of attention heads in the view call causing a shape mismatch at runtime",
    "diff_snippet": "-        x = x.view(*new_shape)  # wrong: 11 heads\n+        x = x.view(*new_shape)  # fixed: 12 heads",
    "buggy_code": "import torch\nimport torch.nn as nn\n\nINPUT_SHAPES = {\"hidden_states\": (2, 8, 768)}\n\n\nclass BuggyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.num_attention_heads = 12\n        self.attention_head_size = 64\n        self.all_head_size = self.num_attention_heads * self.attention_head_size\n        self.query = nn.Linear(768, self.all_head_size)\n        self.key = nn.Linear(768, self.all_head_size)\n        self.value = nn.Linear(768, self.all_head_size)\n\n    def transpose_for_scores(self, x):\n        batch_size, seq_len, _ = x.size()\n        new_shape = (batch_size, seq_len, 11, self.attention_head_size)\n        x = x.view(*new_shape)\n        return x.permute(0, 2, 1, 3)\n\n    def forward(self, hidden_states):\n        q = self.transpose_for_scores(self.query(hidden_states))\n        k = self.transpose_for_scores(self.key(hidden_states))\n        v = self.transpose_for_scores(self.value(hidden_states))\n        scores = torch.matmul(q, k.transpose(-1, -2))\n        return torch.matmul(scores, v)\n"
  },
  {
    "commit_sha": "2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c",
    "pr_number": 20102,
    "family": "bert",
    "title": "Fix dim mismatch in BertIntermediate reshape",
    "body": "BertIntermediate reshapes hidden states to wrong size 769 before dense projection causing a size mismatch error",
    "diff_snippet": "-        x = hidden_states.reshape(batch_size * seq_len, 769)\n+        x = hidden_states.reshape(batch_size * seq_len, 768)",
    "buggy_code": "import torch\nimport torch.nn as nn\n\nINPUT_SHAPES = {\"hidden_states\": (2, 8, 768)}\n\n\nclass BuggyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.dense = nn.Linear(769, 3072)\n\n    def forward(self, hidden_states):\n        batch_size, seq_len, _ = hidden_states.shape\n        x = hidden_states.reshape(batch_size * seq_len, 769)\n        return self.dense(x).view(batch_size, seq_len, 3072)\n"
  },
  {
    "commit_sha": "3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d",
    "pr_number": 20103,
    "family": "bert",
    "title": "Fix transpose causing dim mismatch in BertOutput dense projection",
    "body": "BertOutput incorrectly transposes dims 1 and 2 before the dense layer so last dim is seq_len instead of hidden_size causing a broadcast error",
    "diff_snippet": "-        hidden_states = hidden_states.transpose(1, 2)\n+        # no transpose needed here",
    "buggy_code": "import torch\nimport torch.nn as nn\n\nINPUT_SHAPES = {\"hidden_states\": (2, 8, 3072), \"input_tensor\": (2, 8, 768)}\n\n\nclass BuggyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.dense = nn.Linear(3072, 768)\n        self.LayerNorm = nn.LayerNorm(768)\n\n    def forward(self, hidden_states, input_tensor):\n        hidden_states = hidden_states.transpose(1, 2)\n        hidden_states = self.dense(hidden_states)\n        return self.LayerNorm(hidden_states + input_tensor)\n"
  },
  {
    "commit_sha": "4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e",
    "pr_number": 20201,
    "family": "gpt2",
    "title": "Fix shape mismatch in GPT-2 attention head view off-by-one in head_dim",
    "body": "GPT-2 attention uses head_dim plus 1 in the view operation causing a dim mismatch since 12 times 65 equals 780 which does not equal 768",
    "diff_snippet": "-        q = q.view(batch, seq, self.n_head, self.head_dim + 1).transpose(1, 2)\n+        q = q.view(batch, seq, self.n_head, self.head_dim).transpose(1, 2)",
    "buggy_code": "import torch\nimport torch.nn as nn\n\nINPUT_SHAPES = {\"x\": (2, 8, 768)}\n\n\nclass BuggyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.n_head = 12\n        self.head_dim = 64\n        self.c_attn = nn.Linear(768, 768 * 3)\n        self.c_proj = nn.Linear(768, 768)\n\n    def forward(self, x):\n        batch, seq, _ = x.shape\n        qkv = self.c_attn(x)\n        q, k, v = qkv.split(768, dim=2)\n        q = q.view(batch, seq, self.n_head, self.head_dim + 1).transpose(1, 2)\n        k = k.view(batch, seq, self.n_head, self.head_dim + 1).transpose(1, 2)\n        v = v.view(batch, seq, self.n_head, self.head_dim + 1).transpose(1, 2)\n        att = torch.matmul(q, k.transpose(-2, -1))\n        y = torch.matmul(att, v)\n        y = y.transpose(1, 2).contiguous().view(batch, seq, 768)\n        return self.c_proj(y)\n"
  },
  {
    "commit_sha": "5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f",
    "pr_number": 20202,
    "family": "gpt2",
    "title": "Fix dim mismatch in GPT-2 MLP reshape before projection",
    "body": "GPT-2 MLP reshapes intermediate activations to size 3073 instead of 3072 causing size mismatch",
    "diff_snippet": "-        h = h.reshape(batch * seq, 3073)\n+        h = h.reshape(batch * seq, 3072)",
    "buggy_code": "import torch\nimport torch.nn as nn\n\nINPUT_SHAPES = {\"x\": (2, 8, 768)}\n\n\nclass BuggyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.c_fc = nn.Linear(768, 3072)\n        self.c_proj = nn.Linear(3072, 768)\n\n    def forward(self, x):\n        batch, seq, _ = x.shape\n        h = self.c_fc(x)\n        h = h.reshape(batch * seq, 3073)\n        h = self.c_proj(h)\n        return h.view(batch, seq, 768)\n"
  },
  {
    "commit_sha": "6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a",
    "pr_number": 20203,
    "family": "gpt2",
    "title": "Fix broadcast mismatch in GPT-2 Conv1D weight shape",
    "body": "GPT-2 Conv1D projection weight has inner dimension 769 instead of 768 causing a matmul shape broadcast mismatch",
    "diff_snippet": "-        return torch.matmul(x, self.weight) + self.bias\n+        return torch.matmul(x, self.weight.t()) + self.bias",
    "buggy_code": "import torch\nimport torch.nn as nn\n\nINPUT_SHAPES = {\"x\": (2, 8, 768)}\n\n\nclass BuggyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.weight = nn.Parameter(torch.zeros(769, 768))\n        self.bias = nn.Parameter(torch.zeros(768))\n\n    def forward(self, x):\n        return torch.matmul(x, self.weight) + self.bias\n"
  },
  {
    "commit_sha": "7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b",
    "pr_number": 20301,
    "family": "t5",
    "title": "Fix shape mismatch in T5Attention wrong n_heads in view",
    "body": "T5 self-attention uses 9 heads in view reshape instead of 8 so 9 times 64 equals 576 which does not equal 512 causing a size mismatch",
    "diff_snippet": "-        q = q.view(batch, seq, 9, self.key_value_proj_dim).transpose(1, 2)\n+        q = q.view(batch, seq, self.n_heads, self.key_value_proj_dim).transpose(1, 2)",
    "buggy_code": "import torch\nimport torch.nn as nn\n\nINPUT_SHAPES = {\"hidden_states\": (2, 8, 512)}\n\n\nclass BuggyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.n_heads = 8\n        self.key_value_proj_dim = 64\n        self.q = nn.Linear(512, 512, bias=False)\n        self.k = nn.Linear(512, 512, bias=False)\n        self.v = nn.Linear(512, 512, bias=False)\n\n    def forward(self, hidden_states):\n        batch, seq, _ = hidden_states.shape\n        q = self.q(hidden_states)\n        k = self.k(hidden_states)\n        v = self.v(hidden_states)\n        q = q.view(batch, seq, 9, self.key_value_proj_dim).transpose(1, 2)\n        k = k.view(batch, seq, 9, self.key_value_proj_dim).transpose(1, 2)\n        v = v.view(batch, seq, 9, self.key_value_proj_dim).transpose(1, 2)\n        scores = torch.matmul(q, k.transpose(-1, -2))\n        return torch.matmul(scores, v)\n"
  },
  {
    "commit_sha": "8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c",
    "pr_number": 20302,
    "family": "t5",
    "title": "Fix dim mismatch in T5DenseReluDense FFN reshape",
    "body": "T5DenseReluDense reshapes intermediate activations to size 2049 instead of 2048 causing a size mismatch when passed to output projection",
    "diff_snippet": "-        h = h.reshape(batch * seq, 2049)\n+        h = h.reshape(batch * seq, 2048)",
    "buggy_code": "import torch\nimport torch.nn as nn\n\nINPUT_SHAPES = {\"x\": (2, 8, 512)}\n\n\nclass BuggyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.wi = nn.Linear(512, 2048, bias=False)\n        self.wo = nn.Linear(2048, 512, bias=False)\n\n    def forward(self, x):\n        batch, seq, _ = x.shape\n        h = self.wi(x)\n        h = h.reshape(batch * seq, 2049)\n        h = self.wo(h)\n        return h.view(batch, seq, 512)\n"
  },
  {
    "commit_sha": "9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d",
    "pr_number": 20401,
    "family": "roberta",
    "title": "Fix dim mismatch in RobertaSelfAttention from wrong permute order",
    "body": "RoBERTa self-attention uses permute 0 2 3 1 instead of 0 2 1 3 swapping seq and head_dim axes causing a shape mismatch in the subsequent matmul",
    "diff_snippet": "-        q = q.permute(0, 2, 3, 1)\n+        q = q.permute(0, 2, 1, 3)",
    "buggy_code": "import torch\nimport torch.nn as nn\n\nINPUT_SHAPES = {\"x\": (2, 8, 768)}\n\n\nclass BuggyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.num_attention_heads = 12\n        self.attention_head_size = 64\n        self.query = nn.Linear(768, 768)\n        self.key = nn.Linear(768, 768)\n        self.value = nn.Linear(768, 768)\n\n    def forward(self, x):\n        batch, seq, _ = x.shape\n        q = self.query(x).view(batch, seq, self.num_attention_heads, self.attention_head_size)\n        k = self.key(x).view(batch, seq, self.num_attention_heads, self.attention_head_size)\n        v = self.value(x).view(batch, seq, self.num_attention_heads, self.attention_head_size)\n        q = q.permute(0, 2, 3, 1)\n        k = k.permute(0, 2, 3, 1)\n        v = v.permute(0, 2, 3, 1)\n        return torch.matmul(q, k)\n"
  },
  {
    "commit_sha": "0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e",
    "pr_number": 20402,
    "family": "roberta",
    "title": "Fix size mismatch in RobertaOutput reshape before dense layer",
    "body": "RobertaOutput reshapes intermediate tensor to in_features 3073 instead of 3072 before dense projection causing a size mismatch",
    "diff_snippet": "-        h = hidden_states.reshape(batch * seq, 3073)\n+        h = hidden_states.reshape(batch * seq, 3072)",
    "buggy_code": "import torch\nimport torch.nn as nn\n\nINPUT_SHAPES = {\"hidden_states\": (2, 8, 3072), \"input_tensor\": (2, 8, 768)}\n\n\nclass BuggyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.dense = nn.Linear(3073, 768)\n        self.LayerNorm = nn.LayerNorm(768)\n\n    def forward(self, hidden_states, input_tensor):\n        batch, seq, _ = hidden_states.shape\n        h = hidden_states.reshape(batch * seq, 3073)\n        h = self.dense(h).view(batch, seq, 768)\n        return self.LayerNorm(h + input_tensor)\n"
  },
  {
    "commit_sha": "1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f",
    "pr_number": 20501,
    "family": "llama",
    "title": "Fix shape mismatch in LlamaAttention GQA key view uses num_heads instead of num_kv_heads",
    "body": "LlamaAttention uses num_heads 32 instead of num_kv_heads 8 when reshaping the key projection output causing a size mismatch since 32 times 128 equals 4096 which does not equal 1024",
    "diff_snippet": "-        k = k.view(batch, seq, self.num_heads, self.head_dim).transpose(1, 2)\n+        k = k.view(batch, seq, self.num_kv_heads, self.head_dim).transpose(1, 2)",
    "buggy_code": "import torch\nimport torch.nn as nn\n\nINPUT_SHAPES = {\"x\": (2, 8, 4096)}\n\n\nclass BuggyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.num_heads = 32\n        self.num_kv_heads = 8\n        self.head_dim = 128\n        self.q_proj = nn.Linear(4096, 4096, bias=False)\n        self.k_proj = nn.Linear(4096, 1024, bias=False)\n        self.v_proj = nn.Linear(4096, 1024, bias=False)\n\n    def forward(self, x):\n        batch, seq, _ = x.shape\n        q = self.q_proj(x)\n        k = self.k_proj(x)\n        v = self.v_proj(x)\n        q = q.view(batch, seq, self.num_heads, self.head_dim).transpose(1, 2)\n        k = k.view(batch, seq, self.num_heads, self.head_dim).transpose(1, 2)\n        v = v.view(batch, seq, self.num_heads, self.head_dim).transpose(1, 2)\n        return torch.matmul(q, k.transpose(-2, -1))\n"
  },
  {
    "commit_sha": "2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a",
    "pr_number": 20502,
    "family": "llama",
    "title": "Fix broadcast mismatch in LlamaMLP SwiGLU gate from spurious transpose",
    "body": "LlamaMLP applies transpose 1 2 to gate activation before element-wise multiply creating a broadcast mismatch between gate batch hidden_dim seq and up batch seq hidden_dim",
    "diff_snippet": "-        gate = gate.transpose(1, 2)\n+        # no transpose needed for element-wise product",
    "buggy_code": "import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\nINPUT_SHAPES = {\"x\": (2, 8, 4096)}\n\n\nclass BuggyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.gate_proj = nn.Linear(4096, 11008, bias=False)\n        self.up_proj = nn.Linear(4096, 11008, bias=False)\n        self.down_proj = nn.Linear(11008, 4096, bias=False)\n\n    def forward(self, x):\n        gate = self.gate_proj(x)\n        up = self.up_proj(x)\n        gate = gate.transpose(1, 2)\n        hidden = F.silu(gate) * up\n        return self.down_proj(hidden)\n"
  },
  {
    "commit_sha": "3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b",
    "pr_number": 20601,
    "family": "bart",
    "title": "Fix size mismatch in BartAttention output view after attention",
    "body": "BartAttention reshapes attention output to embed_dim plus 1 equals 1025 instead of 1024 causing a size mismatch with output projection linear layer",
    "diff_snippet": "-        attn_out = attn_out.view(batch, seq, 1025)\n+        attn_out = attn_out.view(batch, seq, self.embed_dim)",
    "buggy_code": "import torch\nimport torch.nn as nn\n\nINPUT_SHAPES = {\"x\": (2, 8, 1024)}\n\n\nclass BuggyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.num_heads = 16\n        self.head_dim = 64\n        self.embed_dim = 1024\n        self.q_proj = nn.Linear(1024, 1024)\n        self.k_proj = nn.Linear(1024, 1024)\n        self.v_proj = nn.Linear(1024, 1024)\n        self.out_proj = nn.Linear(1024, 1024)\n\n    def forward(self, x):\n        batch, seq, _ = x.shape\n        q = self.q_proj(x).view(batch, seq, self.num_heads, self.head_dim).permute(0, 2, 1, 3)\n        k = self.k_proj(x).view(batch, seq, self.num_heads, self.head_dim).permute(0, 2, 1, 3)\n        v = self.v_proj(x).view(batch, seq, self.num_heads, self.head_dim).permute(0, 2, 1, 3)\n        att = torch.matmul(q, k.transpose(-2, -1))\n        attn_out = torch.matmul(att, v)\n        attn_out = attn_out.permute(0, 2, 1, 3).contiguous()\n        attn_out = attn_out.view(batch, seq, 1025)\n        return self.out_proj(attn_out)\n"
  },
  {
    "commit_sha": "4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b3c",
    "pr_number": 20701,
    "family": "distilbert",
    "title": "Fix shape mismatch in DistilBERT MultiHeadSelfAttention head view",
    "body": "DistilBERT MultiHeadSelfAttention uses dim_per_head plus 1 equals 65 in view call so 12 times 65 equals 780 which does not equal 768 causing a shape mismatch",
    "diff_snippet": "-            return t.view(batch, seq, self.n_heads, self.dim_per_head + 1).transpose(1, 2)\n+            return t.view(batch, seq, self.n_heads, self.dim_per_head).transpose(1, 2)",
    "buggy_code": "import torch\nimport torch.nn as nn\n\nINPUT_SHAPES = {\"x\": (2, 8, 768)}\n\n\nclass BuggyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.n_heads = 12\n        self.dim = 768\n        self.dim_per_head = 64\n        self.q_lin = nn.Linear(768, 768)\n        self.k_lin = nn.Linear(768, 768)\n        self.v_lin = nn.Linear(768, 768)\n        self.out_lin = nn.Linear(768, 768)\n\n    def forward(self, x):\n        batch, seq, _ = x.shape\n\n        def shape(t):\n            return t.view(batch, seq, self.n_heads, self.dim_per_head + 1).transpose(1, 2)\n\n        q = shape(self.q_lin(x))\n        k = shape(self.k_lin(x))\n        v = shape(self.v_lin(x))\n        scores = torch.matmul(q, k.transpose(-1, -2))\n        weights = torch.softmax(scores, dim=-1)\n        context = torch.matmul(weights, v)\n        context = context.transpose(1, 2).contiguous().view(batch, seq, self.dim)\n        return self.out_lin(context)\n"
  },
  {
    "commit_sha": "5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b3c4d",
    "pr_number": 20702,
    "family": "distilbert",
    "title": "Fix dim mismatch in DistilBERT FFN from spurious transpose before lin2",
    "body": "DistilBERT FFN applies transpose 1 2 to intermediate activations before second linear layer making last dimension seq equals 8 instead of 3072 causing a size mismatch",
    "diff_snippet": "-        h = h.transpose(1, 2)\n+        # no transpose here",
    "buggy_code": "import torch\nimport torch.nn as nn\n\nINPUT_SHAPES = {\"x\": (2, 8, 768)}\n\n\nclass BuggyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.lin1 = nn.Linear(768, 3072)\n        self.lin2 = nn.Linear(3072, 768)\n\n    def forward(self, x):\n        batch, seq, _ = x.shape\n        h = self.lin1(x)\n        h = h.transpose(1, 2)\n        return self.lin2(h)\n"
  }
]