# Soundness / incompleteness boundary — validated against real code

> Generated by `reproducibility/soundness_boundary.py`. Verdicts only, no timing — byte-deterministic, checked by `reproduce_all.py --check`.

Probes: **4** · modes: `sound`, `balanced`, `heuristic` · all observed verdicts match the documented contract: **TRUE**

| Probe | Contract region | `sound` | `balanced` | `heuristic` | matches contract |
|-------|-----------------|---------|------------|-------------|------------------|
| `in_fragment_clean` | verification soundness (in-fragment, modeled scope) | SAFE | SAFE | SAFE | yes |
| `in_fragment_shape_bug` | refutation soundness (no false alarm) | UNSAFE | UNSAFE | UNSAFE | yes |
| `out_of_fragment_data_dependent_control_flow` | fragment boundary / KNOWN_UNSOUNDNESS U1 (mode-dependent) | UNKNOWN | SAFE | SAFE | yes |
| `out_of_fragment_tensor_to_scalar` | fragment boundary / KNOWN_UNSOUNDNESS U1 (mode-dependent) | UNKNOWN | SAFE | SAFE | yes |

## What each probe proves

- **`in_fragment_clean`** — Clean MLP fully inside the verifiable fragment is SAFE in every mode.
- **`in_fragment_shape_bug`** — Real in-fragment Linear inner-dim mismatch is REFUTED in every mode (Z3-discharged).
- **`out_of_fragment_data_dependent_control_flow`** — Data-dependent branch leaves the fragment: sound mode abstains (UNKNOWN); balanced/heuristic report SAFE (the U1 recall trade-off).
- **`out_of_fragment_tensor_to_scalar`** — `.item()` coerces a tensor to a Python scalar (out of fragment): sound mode abstains; permissive modes report SAFE.

## Contract coverage (from `src/soundness_contract.py`)

- Domain clauses: **5**
- Under-approximated bug classes (may miss bugs, out of the never-miss-pass guarantee): **3**
- Out-of-fragment construct classes (detected by `check_traceability`): **11**
- Known unsoundness gaps surfaced (not hidden): **2** (U1, U2)

The full clause-by-clause contract is `SOUNDNESS_CONTRACT.md` (generated from the same module). This harness shows the *empirical* half: the documented behaviour reproduces on the live verifier.
