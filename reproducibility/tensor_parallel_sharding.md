# Tensor-parallel checker — static verdict vs real torch

> Generated by `reproducibility/tensor_parallel_sharding.py`. Boolean outcomes and verdicts only, no timing or raw floats — byte-deterministic, checked by `reproduce_all.py --check`.

Seed **0** · cases: **10** · static verdict matches real sharded execution on every case: **TRUE**

| Case | Kind | tp | Static issues | Reproduces reference? | static ✓ | runtime ✓ |
|------|------|----|---------------|-----------------------|----------|-----------|
| `megatron_mlp_tp2_correct` | mlp | 2 | (ok) | yes | yes | yes |
| `megatron_mlp_tp4_correct` | mlp | 4 | (ok) | yes | yes | yes |
| `comm_flag_mismatch_gather_then_parallel` | mlp | 2 | comm_flag_mismatch | no — runtime shape error: mat1 and mat2 shapes cannot be multiplied (3x16  | yes | yes |
| `indivisible_ffn15_tp2` | mlp | 2 | indivisible_shard | no — ffn not divisible by tp (no even shard) | yes | yes |
| `inner_dim_mismatch` | mlp | 2 | inner_dim_mismatch | (structural) | yes | yes |
| `hf_style_gqa_sharded_kv_head_dim_independent` | attention | 2 | (ok) | yes | yes | yes |
| `megatron_style_mqa_replicated_kv` | attention | 4 | (ok) | yes | yes | yes |
| `real_transformers_llama_projection_shapes` | attention | 4 | (ok) | yes | yes | yes |
| `kv_heads_neither_shard_nor_replicate` | attention | 4 | kv_head_tp_incompatible | (structural) | yes | yes |
| `sequence_parallel_hidden_axis_layernorm` | attention | 4 | sequence_parallel_axis | (structural) | yes | yes |

The canonical Megatron MLP (ColumnParallel `gather_output=False` → RowParallel `input_is_parallel=True`) reproduces the unsharded forward exactly (up to fp tolerance) at tp=2 and tp=4. Every inconsistent config — gathered output fed to a parallel-input row layer, an indivisible shard, or a contracted-dimension mismatch — is flagged statically and also fails when actually sharded across ranks. The checker lives in `src/tensor_parallel_checks.py`.

The attention cases extend the same proof pattern to MQA/GQA and sequence-parallel LayerNorm. A HuggingFace-style GQA module with independent `head_dim`, a Megatron-style MQA module with replicated KV heads, and an installed `transformers` `LlamaAttention` projection-shape check all agree with the static contract; invalid KV head partitions and hidden-axis sequence-parallel LayerNorm are refuted structurally.
