# Tensor-parallel sharding checker — static verdict vs real torch

> Generated by `reproducibility/tensor_parallel_sharding.py`. Boolean outcomes and verdicts only, no timing or raw floats — byte-deterministic, checked by `reproduce_all.py --check`.

Seed **0** · cases: **5** · static verdict matches real sharded execution on every case: **TRUE**

| Case | tp | Static issues | Reproduces reference? | static ✓ | runtime ✓ |
|------|----|---------------|-----------------------|----------|-----------|
| `megatron_mlp_tp2_correct` | 2 | (ok) | yes | yes | yes |
| `megatron_mlp_tp4_correct` | 4 | (ok) | yes | yes | yes |
| `comm_flag_mismatch_gather_then_parallel` | 2 | comm_flag_mismatch | no — runtime shape error: mat1 and mat2 shapes cannot be multiplied (3x16  | yes | yes |
| `indivisible_ffn15_tp2` | 2 | indivisible_shard | no — ffn not divisible by tp (no even shard) | yes | yes |
| `inner_dim_mismatch` | 2 | inner_dim_mismatch | (structural) | yes | yes |

The canonical Megatron MLP (ColumnParallel `gather_output=False` → RowParallel `input_is_parallel=True`) reproduces the unsharded forward exactly (up to fp tolerance) at tp=2 and tp=4. Every inconsistent config — gathered output fed to a parallel-input row layer, an indivisible shard, or a contracted-dimension mismatch — is flagged statically and also fails when actually sharded across ranks. The checker lives in `src/tensor_parallel_checks.py`.
