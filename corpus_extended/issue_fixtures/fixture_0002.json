{
  "issue_id": "fixture-0002",
  "source": "offline-fixture",
  "title": "Conv2d channel mismatch between two conv layers",
  "labels": ["module: nn", "module: convolution"],
  "url_reference": "https://github.com/pytorch/pytorch/issues/179931",
  "reported_error_substring": "weight of size",
  "input_shapes": {"x": [2, 3, 16, 16]},
  "body": "Stacking two convs throws at runtime:\n\n```python\nimport torch.nn as nn\nimport torch.nn.functional as F\n\nclass M(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.c1 = nn.Conv2d(3, 16, 3, padding=1)\n        self.c2 = nn.Conv2d(8, 32, 3, padding=1)\n\n    def forward(self, x):\n        return self.c2(F.relu(self.c1(x)))\n```\n\nThe second conv declares 8 input channels but receives 16."
}
