# Step 15 -- differential fuzzing for false positives

A random-architecture fuzzer grows valid `nn.Module`s by threading the running tensor shape through each op (Linear / Conv2d / BatchNorm / LayerNorm / pooling / flatten / activations), validates each one **executes without error** in eager PyTorch, then asserts TensorGuard in strict `sound` mode never Refutes a cleanly-executing model. Generated by `evaluation/diff_fuzz.py` (fully seeded).

## Result

| Metric | Value |
|---|---|
| Random seeds attempted | 200 |
| Admitted (executed clean) | 200 |
| Skipped (did not execute) | 0 |
| **False positives (Refuted clean code)** | **0** |
| Verified SAFE | 200 |
| Abstained | 0 |
| SAFE coverage | 1.000 |

Across the admitted, cleanly-executing random models TensorGuard produced **0 false positives**. Coverage (the share verified SAFE rather than abstained) is 1.000, so the zero-false-positive result is not a vacuous "always abstain".

Rank breakdown of admitted models: rank-2 (vector) 94, rank-4 (image) 106.
